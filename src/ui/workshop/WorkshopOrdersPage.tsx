import { Box, Typography } from "@mui/material";

export function WorkshopOrdersPage() {
  return (
    <Box sx={{ p: 2 }}>
      <Typography variant="h6" fontWeight={700}>
        Taller — Órdenes
      </Typography>
      <Typography variant="body2" color="text.secondary">
        Placeholder: filtro y búsqueda.
      </Typography>
    </Box>
  );
}
